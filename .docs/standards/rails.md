# Rails

We keep everything rails. From the backend to the frontend. We should use rails. So make sure to understand the documentation in `https://guides.rubyonrails.org/`.

Take a moment to read the `Turbo` and `StimulusJS` docs too. Here's the documentation: `https://turbo.hotwired.dev/`. Here is the github repository of turbo-rails `https://github.com/hotwired/turbo-rails?tab=readme-ov-file`.


